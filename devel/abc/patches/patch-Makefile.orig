$OpenBSD$
--- Makefile.orig	Wed Sep 16 17:19:23 2015
+++ Makefile	Wed Sep 16 17:43:01 2015
@@ -70,8 +70,12 @@ endif
 
 endif
 
+ifneq ($(findstring OpenBSD, $(shell uname)), OpenBSD)
+   LIBS += -ltermcap
+endif
+
 # LIBS := -ldl -lrt
-LIBS += -ldl -lm
+LIBS += -lm
 ifneq ($(findstring Darwin, $(shell uname)), Darwin)
    LIBS += -lrt
 endif
